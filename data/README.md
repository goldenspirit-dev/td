# data/

This folder is gitignored. Expected layout:

- `raw/` — original downloaded volumes (e.g. `raw/kanzi/*.nii.gz`)
- `interim/` — intermediate preprocessing outputs
- `processed/` — analysis-ready volumes & feature arrays
- `synthetic/` — generated phantoms for pipeline validation
